source 'https://rubygems.org'

# define ruby version
ruby '1.9.3', :engine => 'jruby', :engine_version => '1.7.10'

# Rails
gem 'rails', '4.0.2'

# HTTParty for calling REST Api
gem 'httparty'

# Use dalli for interactions with memcached
gem 'dalli'

# tests
group :development, :test do
  gem 'rspec-rails'
  gem 'warbler'
end
